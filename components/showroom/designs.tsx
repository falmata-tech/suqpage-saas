"use client";
import type { Catalog, Product } from "@/lib/types";

export type DesignProps={catalog:Catalog;products:Product[];filter:string;setFilter:(v:string)=>void;query:string;setQuery:(v:string)=>void;openProduct:(p:Product)=>void;addProduct:(p:Product)=>void;cartCount:number;openCart:()=>void};
const status:Record<string,string>={available:"Available",limited:"Limited",unavailable:"Unavailable",coming_soon:"Coming soon"};
function Filters({p}:{p:DesignProps}){return <><input className="sr-search" aria-label="Search products" value={p.query} onChange={e=>p.setQuery(e.target.value)} placeholder="Search products"/><div className="sr-filters"><button className={`sr-filter ${p.filter==="all"?"active":""}`} onClick={()=>p.setFilter("all")}>All</button>{p.catalog.categories.filter(c=>c.is_active).map(c=><button key={c.id} className={`sr-filter ${p.filter===String(c.id)?"active":""}`} onClick={()=>p.setFilter(String(c.id))}>{c.name}</button>)}</div></>}
function Logo({p}:{p:DesignProps}){return <a className="sr-logo" href="#top"><img src={p.catalog.business.logo_path} alt=""/><span>{p.catalog.business.name}</span></a>}
function Footer({p}:{p:DesignProps}){return <footer className="sr-footer"><div className="sr-wrap sr-footer-row"><strong>{p.catalog.business.name}</strong><span>@{p.catalog.business.handle}</span><a href="/">Powered by SuqPage</a></div></footer>}

export function AlHayaDesign(p:DesignProps){const b=p.catalog.business;return <div className="showroom theme-alhaya"><div className="sr-wrap"><header className="sr-header"><Logo p={p}/><nav className="sr-nav"><a href="#collection">Collections</a><a href="#story">Our story</a><button className="sr-cart-trigger" onClick={p.openCart}>Inquiry · {p.cartCount}</button></nav></header></div><main><section className="sr-wrap sr-hero" id="top"><div className="sr-hero-copy"><span className="sr-kicker">{b.tagline}</span><h1>{b.hero_title}</h1><p>{b.hero_subtitle}</p><button className="sr-cart-trigger" onClick={()=>document.getElementById("collection")?.scrollIntoView()}>Enter the collection</button></div><div className="sr-hero-media"><img src={b.hero_image_path} alt={b.name}/></div></section><section className="sr-section" id="collection"><div className="sr-wrap"><div className="sr-section-head"><div><span className="sr-kicker">The house collection</span><h2>{p.catalog.collections[0]?.name}</h2></div><p>Choose a piece, select its original color and size names, and add it to one clear inquiry.</p></div><Filters p={p}/><div className="sr-products">{p.products.map(product=><article className="sr-card" key={product.id}><button className="sr-card-media" onClick={()=>p.openProduct(product)}><span className="sr-status">{status[product.availability]}</span><img src={product.image_path} alt={product.name}/></button><div className="sr-card-copy"><small>{product.eyebrow}</small><h3>{product.name}</h3><p>{product.description}</p><div className="sr-card-actions"><button onClick={()=>p.openProduct(product)}>View piece</button><button className="add" onClick={()=>product.option_groups?.length?p.openProduct(product):p.addProduct(product)}>Add</button></div></div></article>)}</div></div></section><section className="sr-section" id="story"><div className="sr-wrap sr-section-head"><div><span className="sr-kicker">Al Haya Brand</span><h2>Modesty with a considered finish.</h2></div><p>{b.description}</p></div></section></main><Footer p={p}/></div>}

export function UsaShopDesign(p:DesignProps){const b=p.catalog.business;return <div className="showroom theme-usashopet"><div className="usa-ribbon">U.S. BEAUTY FINDS · ONE SIMPLE INQUIRY · HUMAN CONFIRMATION</div><div className="sr-wrap"><header className="sr-header"><Logo p={p}/><nav className="sr-nav"><a href="#shelf">Shop the shelf</a><a href="#promise">How it works</a><button className="sr-cart-trigger" onClick={p.openCart}>Bag {p.cartCount}</button></nav></header><section className="sr-hero" id="top"><div className="sr-hero-copy"><span className="sr-kicker">Fresh from the U.S.</span><h1>{b.hero_title}</h1><p>{b.hero_subtitle}</p><button className="sr-cart-trigger" onClick={()=>document.getElementById("shelf")?.scrollIntoView()}>Browse the shelf</button></div><div className="sr-hero-media"><img src={b.hero_image_path} alt={b.name}/><span className="usa-sticker">Beauty<br/>drop!</span></div></section></div><section className="sr-section" id="shelf"><div className="sr-wrap"><div className="sr-section-head"><div><span className="sr-kicker">The colorful shelf</span><h2>Skincare, wellness and self-care.</h2></div><p>Tap a category or search by the exact product name you already know.</p></div><Filters p={p}/><div className="sr-products">{p.products.map(product=><article className="sr-card" key={product.id}><button className="sr-card-media" onClick={()=>p.openProduct(product)}><span className="sr-status">{status[product.availability]}</span><img src={product.image_path} alt={product.name}/></button><div className="sr-card-copy"><small>{product.category_name}</small><h3>{product.name}</h3><p>{product.description}</p><div className="sr-card-actions"><button onClick={()=>p.openProduct(product)}>Details</button><button className="add" onClick={()=>product.option_groups?.length?p.openProduct(product):p.addProduct(product)}>Add to bag</button></div></div></article>)}</div></div></section><section className="usa-promise" id="promise"><div className="sr-wrap"><span className="sr-kicker">The USAshopET promise</span><h2>Build one list. Ask once. Confirm directly.</h2><button className="sr-cart-trigger" onClick={p.openCart}>Open your inquiry</button></div></section><Footer p={p}/></div>}

export function NovaTechDesign(p:DesignProps){const b=p.catalog.business;const featured=p.products[0]||p.catalog.products[0];return <div className="showroom theme-novatech"><div className="sr-wrap"><header className="sr-header"><Logo p={p}/><nav className="sr-nav"><a href="#flagships">Flagships</a><a href="#standard">Nova standard</a><button className="sr-cart-trigger" onClick={p.openCart}>Inquiry {p.cartCount}</button></nav></header></div><main><section className="nova-stage" id="top"><div className="sr-wrap"><div className="nova-stage-copy"><span className="sr-kicker">The next generation is here</span><h1>{b.hero_title}</h1><p>{b.hero_subtitle}</p><div><button className="sr-cart-trigger" onClick={()=>document.getElementById("flagships")?.scrollIntoView()}>Explore flagships</button><button className="nova-link" onClick={p.openCart}>Build an inquiry →</button></div></div><img src={b.hero_image_path} alt={featured?.name||b.name}/></div></section><section className="sr-section" id="flagships"><div className="sr-wrap"><div className="sr-section-head"><div><span className="sr-kicker">The collection</span><h2>Only category-defining products.</h2></div><p>Specifications, configurations and availability are managed dynamically by NovaTech.</p></div><Filters p={p}/><div className="sr-products">{p.products.map(product=><article className="sr-card" key={product.id}><button className="sr-card-media" onClick={()=>p.openProduct(product)}><span className="sr-status">{status[product.availability]}</span><img src={product.image_path} alt={product.name}/></button><div className="sr-card-copy"><small>{product.eyebrow}</small><h3>{product.name}</h3><p>{product.description}</p><div className="sr-card-actions"><button onClick={()=>p.openProduct(product)}>Explore</button><button className="add" onClick={()=>product.option_groups?.length?p.openProduct(product):p.addProduct(product)}>Add to inquiry</button></div></div></article>)}</div></div></section><section className="nova-standard" id="standard"><div className="sr-wrap"><span className="sr-kicker">Nova standard</span><h2>Premium technology deserves a precise buying conversation.</h2><div className="nova-points"><div><strong>01</strong><h3>Curated flagships</h3><p>A focused selection instead of an endless catalog.</p></div><div><strong>02</strong><h3>One clear inquiry</h3><p>Configurations and quantities travel together.</p></div><div><strong>03</strong><h3>Human confirmation</h3><p>Availability and delivery are confirmed directly.</p></div></div></div></section></main><Footer p={p}/></div>}

export function HomeVibeDesign(p:DesignProps){const b=p.catalog.business;return <div className="showroom theme-homevibe"><div className="sr-wrap"><header className="sr-header"><Logo p={p}/><nav className="sr-nav"><a href="#edit">The edit</a><a href="#journal">Journal</a><button className="sr-cart-trigger" onClick={p.openCart}>Saved pieces · {p.cartCount}</button></nav></header><section className="sr-hero" id="top"><div className="sr-hero-copy"><span className="sr-kicker">The considered home</span><h1>{b.hero_title}</h1><p>{b.hero_subtitle}</p><button className="sr-cart-trigger" onClick={()=>document.getElementById("edit")?.scrollIntoView()}>Explore the home edit</button></div><div className="sr-hero-media"><img src={b.hero_image_path} alt={b.name}/></div></section></div><section className="home-note"><div className="sr-wrap"><p>Useful objects. Recognizable makers. A home that feels more intentional.</p></div></section><section className="sr-section" id="edit"><div className="sr-wrap"><div className="sr-section-head"><div><span className="sr-kicker">Room by room</span><h2>The HomeVibe edit.</h2></div><p>Browse by room or search a known product. Each inquiry keeps the exact finish, size and specification you choose.</p></div><Filters p={p}/><div className="sr-products">{p.products.map(product=><article className="sr-card" key={product.id}><button className="sr-card-media" onClick={()=>p.openProduct(product)}><span className="sr-status">{status[product.availability]}</span><img src={product.image_path} alt={product.name}/></button><div className="sr-card-copy"><small>{product.eyebrow}</small><h3>{product.name}</h3><p>{product.description}</p><div className="sr-card-actions"><button onClick={()=>p.openProduct(product)}>Read details</button><button className="add" onClick={()=>product.option_groups?.length?p.openProduct(product):p.addProduct(product)}>Save piece</button></div></div></article>)}</div></div></section><section className="home-journal" id="journal"><div className="sr-wrap"><span className="sr-kicker">Home notes</span><h2>Buy fewer things. Choose the useful ones well.</h2><p>{b.description}</p></div></section><Footer p={p}/></div>}
