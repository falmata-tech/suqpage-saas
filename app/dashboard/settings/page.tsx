import DashboardShell from "@/components/DashboardShell";import {updateBusinessAction} from "@/app/actions";import {requireUser} from "@/lib/auth";import {resolveManagedBusiness as resolveBusiness} from "@/lib/dashboard";
export const dynamic="force-dynamic";export default async function SettingsPage({searchParams}:{searchParams:Promise<{business?:string;saved?:string;error?:string}>}){const user=await requireUser();const p=await searchParams;const business=resolveBusiness(user,p.business);if(!business)return null;return <DashboardShell user={user} business={business}><div className="dashboard-head"><div><h1>Business settings</h1><p>Content and contacts remain dynamic while SuqPage manages the custom renderer.</p></div></div>{p.saved&&<p className="notice">Business settings saved.</p>}{p.error&&<p className="error">{p.error}</p>}<form className="panel form-grid" action={updateBusinessAction} encType="multipart/form-data"><input type="hidden" name="businessId" value={business.id}/><div className="field"><label>Business name</label><input aria-label="Business name" required name="name" defaultValue={business.name}/></div><div className="field"><label>Assigned design</label><input aria-label="Assigned design" value={business.design_key} disabled/></div><div className="field full"><label>Tagline</label><input aria-label="Tagline" name="tagline" defaultValue={business.tagline}/></div><div className="field full"><label>Description</label><textarea aria-label="Description" name="description" defaultValue={business.description}/></div><div className="field"><label>Hero title</label><input aria-label="Hero title" name="heroTitle" defaultValue={business.hero_title}/></div><div className="field"><label>Hero subtitle</label><input aria-label="Hero subtitle" name="heroSubtitle" defaultValue={business.hero_subtitle}/></div><div className="field"><label>Logo</label><input aria-label="Logo" type="file" name="logo" accept="image/jpeg,image/png,image/webp"/></div><div className="field"><label>Hero image</label><input aria-label="Hero image" type="file" name="heroImage" accept="image/jpeg,image/png,image/webp"/></div><div className="field"><label>Browser icon</label><input aria-label="Browser icon" type="file" name="favicon" accept="image/jpeg,image/png,image/webp"/></div><div className="field"><label>Notification email</label><input aria-label="Notification email" type="email" name="contactEmail" defaultValue={business.contact_email}/></div><div className="field"><label>WhatsApp</label><input aria-label="WhatsApp" name="whatsapp" defaultValue={business.whatsapp} placeholder="251911234567"/></div><div className="field"><label>Telegram</label><input aria-label="Telegram" name="telegram" defaultValue={business.telegram} placeholder="AlHayaModest"/></div><div className="field"><label>TikTok</label><input aria-label="TikTok" name="tiktok" defaultValue={business.tiktok} placeholder="alhayabrand"/></div><div className="field"><label>Page title</label><input aria-label="Page title" name="siteTitle" defaultValue={business.site_title||business.name}/></div><div className="field full"><label>Search and sharing description</label><textarea aria-label="Search and sharing description" name="siteDescription" defaultValue={business.site_description||business.description}/></div><div className="field full"><small>Images are limited to valid JPEG, PNG or WebP files of 5 MB or less.</small></div><div className="field full"><button className="btn brand">Save business settings</button></div></form></DashboardShell>}
