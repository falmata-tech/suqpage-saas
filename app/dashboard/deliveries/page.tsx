import DashboardShell from "@/components/DashboardShell";import {createDeliveryRequestAction} from "@/app/actions";import {requireUser} from "@/lib/auth";import {resolveBusiness} from "@/lib/dashboard";import {getInquiry,listDeliveryCompanies,listDeliveryRequests,listInquiries} from "@/lib/db";
export const dynamic="force-dynamic";export default async function DeliveriesPage({searchParams}:{searchParams:Promise<{business?:string;inquiry?:string;created?:string;error?:string}>}){const user=await requireUser();const p=await searchParams;const business=resolveBusiness(user,p.business);if(!business)return null;const companies=listDeliveryCompanies();const inquiries=listInquiries(business.id);const chosen=p.inquiry?getInquiry(Number(p.inquiry),business.id):undefined;const deliveries=listDeliveryRequests(business.id);return <DashboardShell user={user} business={business}><div className="dashboard-head"><div><h1>Delivery requests</h1><p>Mock Malikt Board integration: send one request to one or several supported delivery companies.</p></div></div>{p.error&&<p className="error">{p.error}</p>}{p.created&&<p className="notice">Delivery request <strong>{p.created}</strong> was submitted to the mock Malikt Board.</p>}<div className="split"><section className="panel"><h2>Initiate delivery</h2><form action={createDeliveryRequestAction} className="form-grid"><input type="hidden" name="businessId" value={business.id}/><div className="field full"><label>Related inquiry</label><select aria-label="Related inquiry" name="inquiryId" defaultValue={chosen?.id||""}><option value="">No related inquiry</option>{inquiries.map((i:any)=><option key={i.id} value={i.id}>{i.customer_name} · #{i.id}</option>)}</select></div><div className="field"><label>Customer name</label><input aria-label="Customer name" required name="customerName" defaultValue={chosen?.customer_name||""}/></div><div className="field"><label>Phone</label><input aria-label="Phone" required name="phone" defaultValue={chosen?.contact||""}/></div><div className="field full"><label>Pickup address</label><input aria-label="Pickup address" required name="pickupAddress" defaultValue="Business pickup location"/></div><div className="field full"><label>Delivery address</label><input aria-label="Delivery address" required name="deliveryAddress" placeholder="Customer delivery area or address"/></div><div className="field"><label>Package count</label><input aria-label="Package count" type="number" min="1" name="packageCount" defaultValue="1"/></div><div className="field full"><label>Delivery note</label><textarea aria-label="Delivery note" name="note" defaultValue={chosen?.note||""}/></div><div className="field full"><label>Send to delivery companies</label>{companies.map((c:any)=><label key={c.id} style={{display:"flex",gap:9,alignItems:"center",padding:"8px 0"}}><input type="checkbox" name="companyIds" value={c.id} defaultChecked={c.slug==="malikt-express"}/><span><strong>{c.name}</strong><br/><small>{c.service_area}</small></span></label>)}</div><div className="field full"><button className="btn brand">Submit to Malikt Board</button></div></form></section><section className="panel"><h2>Mock API contract</h2><p>The dashboard reads supported companies from the same data exposed at:</p><div className="sdk-code">GET /api/malikt/companies<br/>POST /api/malikt/requests</div><p style={{color:"var(--muted)"}}>The real Malikt Board can later replace this adapter without changing the custom showroom designs.</p></section></div><section className="panel"><h2>Request history</h2>{deliveries.length?<div className="table-wrap"><table className="data-table"><thead><tr><th>Request</th><th>Customer</th><th>Route</th><th>Packages</th><th>Status</th><th>Created</th></tr></thead><tbody>{deliveries.map((d:any)=><tr key={d.id}><td><strong>{d.external_request_id}</strong></td><td>{d.customer_name}<br/><small>{d.phone}</small></td><td>{d.pickup_address}<br/>→ {d.delivery_address}</td><td>{d.package_count}</td><td><span className={`badge ${d.status}`}>{d.status}</span></td><td>{new Date(d.created_at).toLocaleString()}</td></tr>)}</tbody></table></div>:<div className="empty-state">No delivery requests yet.</div>}</section></DashboardShell>}
