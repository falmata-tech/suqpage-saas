import {notFound} from "next/navigation";import DashboardShell from "@/components/DashboardShell";import ProductForm from "@/components/ProductForm";import {updateProductAction} from "@/app/actions";import {requireUser} from "@/lib/auth";import {resolveManagedBusiness as resolveBusiness} from "@/lib/dashboard";import {getCatalogByBusinessId} from "@/lib/db";
export const dynamic="force-dynamic";export default async function EditProduct({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{business?:string;saved?:string;error?:string}>}){const user=await requireUser();const p=await searchParams;const route=await params;const business=resolveBusiness(user,p.business);if(!business)return null;const catalog=getCatalogByBusinessId(business.id,true)!;const product=catalog.products.find(x=>x.id===Number(route.id));if(!product)notFound();return <DashboardShell user={user} business={business}><div className="dashboard-head"><div><h1>Edit {product.name}</h1><p>Product names and entered option values are never translated automatically.</p></div></div>{p.saved&&<p className="notice">Product saved.</p>}{p.error&&<p className="error">{p.error}</p>}<ProductForm catalog={catalog} product={product} action={updateProductAction} businessId={business.id}/></DashboardShell>}
